# VITA 메이커톤 시연 점검 체크리스트

> 2026-07-29: 로컬에서 실제 백엔드(Supabase 연결)+Expo 웹으로 앱을 띄워 자동 점검 가능한 항목을 확인함. 결과는 각 항목에 `[검증됨]`/`[수정함]`으로 표시. 행사장 WiFi·실물 하드웨어·발표 리허설처럼 그 자리에 있어야만 확인 가능한 항목은 그대로 남겨둠.

목표는 실서비스 배포가 아니라 **대회 당일 데모가 처음부터 끝까지 끊김 없이 실제로 작동하는 것**. 보안/스토어 심사/CI 같은 항목은 이 단계에서 불필요하므로 빼고, "그 자리에서 시연했을 때 안 터지는가"만 기준으로 정리했다.

우선순위: 🔴 이거 안 되면 데모 자체가 무너짐 · 🟡 되면 좋음(임팩트용) · 🟢 여유 있으면

---

## 1. 🔴 해피패스 시연 시나리오 통독

발표에서 실제로 보여줄 화면 순서대로 앱을 처음부터 끝까지 한 번 직접 눌러본다. 중간에 로딩이 멈추거나 빈 화면이 뜨는 곳이 없는지 확인.

- [x] `[검증됨]` 로컬 백엔드(실제 Supabase 연결) + Expo 웹으로 9개 화면 직접 클릭 순회 완료: 메인 → 스마트홈 제어 → 캘린더 → 에너지 사용량 → 화재 예방 시스템(+시뮬레이션 버튼) → 자동화 규칙 → 취침 모드 관리 → 수면 통계 → 생활 패턴 분석. 전 화면 콘솔 에러 0건, 크래시 없음
- [x] `[수정함]` 메인화면 시계 카드의 요일 라벨이 `"Wen"`으로 **하드코딩**되어 있던 버그 발견 → 실제 날짜 기준으로 계산하도록 수정 (`src/screens/MainScreen.tsx`). 고치기 전엔 데모 당일 요일이 뭐든 상관없이 항상 "Wen"이 찍혀서 심사위원 눈에 띄기 쉬운 티가 났을 것
- [x] `[수정함]` 메인화면 "내부 습도" 값이 `51.170183333333334%`처럼 소수점이 끝없이 이어져 카드 레이아웃이 깨지는 버그 발견 → 온도(`toFixed(1)`)와 똑같이 소수 첫째자리로 반올림하도록 수정 (`src/screens/MainScreen.tsx`). 실제 백엔드(`/home/summary`)가 여러 센서 값을 평균 내서 주는 값이라 실기기 붙이면 이런 긴 소수가 나올 가능성이 높음 — 고치기 전엔 실제 센서 연결 후 데모에서 바로 드러났을 버그. FirePreventionScreen 쪽 더미 온습도는 자체적으로 이미 반올림하고 있어서 문제없음(확인함)
- [x] `[검증됨]` `/rooms`, `/home/summary`, `/schedule/*`, `/notifications`, `/settings`, `/automation-rules`, `/sleep/*`, `/pattern/*`, `/energy/usage` 등 앱이 쓰는 백엔드 엔드포인트 전부 실제 Supabase 대상으로 호출해 200 응답 확인 (`backend/tests`는 인메모리 fake라 이번엔 실제 DB로 별도 확인함)
- [ ] 실기기(EAS 빌드)에서도 동일하게 순회 — 이번 확인은 Expo **웹**으로 진행했어서, 실기기 전용 동작(카메라 권한, 네이티브 제스처 등)은 별도로 한 번 더 확인 필요
- [ ] 목업 상태로 남아있는 화면(`EnergyTreeScreen`, `GuidebookScreen`, `BillReceiptScreen` 등 일부)이 시연 시나리오에 들어간다면, "목업이어도 자연스럽게 보이는지"만 체크하면 충분 — 실제 API 연동 여부는 이 단계에서 중요하지 않음

## 2. 🔴 네트워크 — 모바일 핫스팟 + 노트북 구성으로 확정

시연 구성: **와이파이는 모바일 핫스팟, 화면은 노트북(웹)**. 이 조합이라 좋아진 점과 새로 생긴 리스크가 있음.

- [x] `[검증됨]` 앱(웹)과 백엔드를 **같은 노트북**에서 띄우면 `.env`의 `EXPO_PUBLIC_API_URL=http://localhost:8000`가 그대로 통함 — LAN IP를 안 맞춰도 됨 (직접 이 구성으로 전 화면 테스트 완료). 행사장 와이파이가 아니라 핫스팟이라 **WPA2-Enterprise 문제도 해당 없음** (ESP32가 못 붙는 기관망 문제는 사라짐)
- [ ] **새 리스크 — Windows 방화벽**: ESP32는 노트북과 다른 물리 기기라 `localhost`가 아니라 노트북의 **핫스팟 IP**로 백엔드를 찾아가야 함. 이 노트북에서 확인해보니 `python.exe` 인바운드 허용 규칙이 있긴 한데 **시스템 파이썬**(`...\Python313\python.exe`) 것이고, 실제 백엔드를 돌리는 `backend\venv\Scripts\python.exe`는 **다른 바이너리라 이 규칙이 안 먹음**. 로컬(localhost) 테스트는 이 문제를 못 잡아내므로(루프백은 방화벽이 별도 취급), 반드시 아래 순서로 현장에서 직접 확인할 것:
  1. 노트북을 핫스팟에 연결 → 백엔드 실행 → "Windows Defender 방화벽이 차단했습니다" 팝업 뜨면 **액세스 허용** 클릭
  2. 팝업이 안 뜨면: 설정 → Windows 보안 → 방화벽 및 네트워크 보호 → 앱이 방화벽을 통과하도록 허용 → `backend\venv\Scripts\python.exe` 직접 추가
  3. 확인 방법: 같은 핫스팟에 붙은 **다른 기기(휴대폰 등)**의 브라우저로 `http://<노트북 핫스팟 IP>:8000/health`가 열리는지 테스트 — 이게 되면 ESP32도 됨
- [ ] ESP32 `firmware/*/config.h`의 `API_BASE_URL`이 지금 집 와이파이 IP(`192.168.200.141`)로 박혀있음 — 핫스팟 연결 시 IP가 바뀌므로 당일 `ipconfig`로 새 IP 확인 후 각 config.h 갱신 + 재업로드 필요. 가능하면 하루 전에 폰 핫스팟으로 미리 리허설 추천
- [ ] 핫스팟 쓰는 폰 배터리/충전 케이블 챙기기, 노트북 절전모드/화면보호기 꺼두기(백엔드가 죽지 않도록)

## 3. 🔴 하드웨어 노드 현장 동작

- [ ] `env_presence_node`(온습도+PIR), `relay_node`(릴레이), `power_monitor_node`(전력계), `presence_vision_node`(카메라 재실감지) — 시연에 쓸 노드들을 현장 전원에 꽂고 서버에 값이 실제로 올라오는지 확인
- [ ] `life_pattern_vision_node`는 아직 Edge Impulse 모델이 미학습 상태 — 이 노드는 시연 시나리오에서 **제외**하거나, 넣을 거면 지금 학습부터 끝내야 함
- [ ] 각 노드의 WiFi 재연결 로직은 있지만(`WiFi.reconnect()`), 그래도 시연 5분 전 전원 재부팅으로 연결 상태 새로 확인하는 걸 루틴으로
- [ ] 노드 전원(배터리/케이블) 넉넉히 확보 — 발표 중 방전되면 복구 불가

### 3-1. 🔴 에어컨/선풍기 등록 — 직접 재현해서 확인한 3가지 함정

`relay_node`로 에어컨·선풍기 같은 실제 가전을 제어할 계획이라면, 등록 경로를 반드시 아래대로 따라야 함. 로컬에서 실제 curl로 재현해서 확인한 내용:

- [x] `[검증됨/위험]` **앱의 "+ 기기 추가" 버튼은 가짜(mock) 기기만 만든다.** `POST /devices/mock-register`를 호출하는데, 백엔드 주석에 "실제 ESP32 없이도 device row를 흉내내는 프로토타입 전용 엔드포인트"라고 명시돼 있음. 이걸로 만든 기기는 토글해도 릴레이에 아무 명령도 안 감. **실제 가전을 켜고 끄려면 이 버튼을 쓰면 안 됨.** (실제로 지금 이 DB에 이 버튼으로 만들어진 방 미배정 고아 기기 3개, 이상한 이름으로 방에 남아있던 테스트 기기 2개를 발견해서 정리함 — 2026-07-29 기준)
- [x] `[검증됨/위험]` **진짜 `relay_node`가 등록해도 앱에 안 보인다.** ESP32가 부팅 시 호출하는 `POST /devices/register`는 항상 `room_id: null`로 기기를 만드는데, `GET /rooms`는 `room_id`가 있는 기기만 돌려줌 (`backend/app/routers/rooms.py:124`). 실제로 재현해봄: 등록 직후 `/rooms` 응답에 없음 → `PATCH /devices/{id}`로 `room_id`(+`name`)를 넣어주니 그제서야 나타남. **앱에는 "미배정 기기를 방에 배정"하는 UI가 없으므로, 각 relay_node를 처음 등록시킨 뒤 아래 curl을 한 번씩 수동으로 날려야 함:**
  ```
  curl -X PATCH http://<백엔드주소>:8000/devices/<config.h의 DEVICE_ID> \
    -H "Content-Type: application/json" \
    -d '{"room_id": <방 ID, /rooms로 조회>, "name": "에어컨"}'
  ```
- [x] `[검증됨]` **이름은 정확히 "에어컨"/"선풍기"로.** `src/utils/energy.ts`의 키워드 매칭에 이미 "에어컨"(1500W 추정), "선풍기"(50W 추정)가 등록돼 있어서, 위 PATCH의 `name`을 정확히 이렇게 지으면 "실시간 기기별 사용 현황" 위젯과 **취침모드 자동화**(`SleepContext.tsx`의 `applyDeviceType('에어컨', ...)` — 실제로 `controlDevice` API를 호출해서 릴레이를 켜고 끔)가 자동으로 인식함. relay_node가 기본으로 붙이는 라벨("거실 기기 제어")은 이 키워드에 안 걸리니 반드시 rename 필요
- [ ] 지금 시드 데이터에 이미 라벨 "에어컨"인 `seed-relay-aircon`(mock, 실제 릴레이 아님)이 방에 들어있음 — 진짜 릴레이를 "에어컨"이라는 같은 이름으로 추가하면 카드가 두 개 뜸. 데모 전에 이 시드 mock 기기를 지우거나 이름을 바꿔서 중복 방지
- [ ] "총 사용량(kWh)" 꺾은선 차트(EnergyUsageScreen 상단)는 `power_monitor_node`(PZEM) 실측값 전용이라, relay_node만 있으면 이 그래프는 계속 0으로 평평함 — "기기별 사용 현황" 목록의 추정 와트값과는 별개 데이터 소스라는 걸 발표 스크립트에서 헷갈리지 않게 주의
- [ ] `FirePreventionScreen`의 "AI 이상 패턴 감지"는 히터·전기장판·가스레인지·난로·온풍기만 고위험 기기로 분류함(코드 확인) — 에어컨/선풍기는 이 감지에 안 걸리는 게 정상 동작이니 "왜 위험 감지가 안 되지?" 하고 당황하지 않기

## 4. 🟡 폴백 계획 — 하드웨어가 그 자리에서 안 붙을 때

- [x] `[검증됨]` 화재 감지: `FirePreventionScreen`의 "화재 상황 시뮬레이션" 버튼 실제로 눌러서 확인 — 누르는 즉시 "모든 방 안전" → "1개 방 위험 감지"로 바뀌고, 방 카드에 45°C·"5분 사이 온도가 20.2°C나 올랐어요" 같은 위험 문구까지 정상 표시됨. 센서 없이도 이 버튼 하나로 화재감지 시연 완전히 대체 가능
- [x] `[검증됨]` 생활 패턴 분석 화면은 모델 미연결 상태를 "- (모델 연결 전)"으로 자연스럽게 표시함 — `life_pattern_vision_node` 미완성 상태로 데모에 들어가도 에러 없이 빈 상태 화면으로 보임
- [ ] 재실/취침 감지: 카메라·PIR가 현장에서 불안정하면 어떤 화면으로 대체 시연할지 미리 정해두기
- [ ] `backend/seed_demo.py`로 방/기기/센서값/전력 사용량 더미 데이터를 미리 채워둘 수 있음 — 실기기 연결이 불안하면 이걸로 메인/스마트홈제어/에너지사용량 화면은 그림이 나옴 (`venv/Scripts/python seed_demo.py`, 여러 번 실행해도 안전). 현재 실제 DB에는 방 1개("ROOM")만 있고 등록된 기기가 없어서, 시연 전에 한 번 돌려서 데이터를 채워두는 걸 권장
- [ ] "혹시 와이파이가 안 되면 이 화면으로 넘어간다" 같은 플랜B를 발표자끼리 말로 맞춰두기

## 5. 🟡 앱 자체 안정성 스모크 테스트

- [x] `[수정함]` 노트북 브라우저처럼 넓은 화면에서 앱이 브라우저 폭 전체로 늘어져 폰 앱 느낌이 안 나던 문제 → **자동으로 폰 비율(430px)로 가운데 정렬**되도록 수정 (`App.tsx`에 웹 전용 프레임 추가, 그리드/차트가 이 폭을 기준으로 계산하도록 `src/hooks/useAppWindowDimensions.ts` 공용 훅 도입). 브라우저 창 크기를 수동으로 줄이거나 개발자도구를 켤 필요 없이 그냥 열면 폰처럼 보임 — 메인/스마트홈제어/에너지사용량 차트/캘린더 그리드 전부 재확인 완료, 콘솔 에러 없음. 실기기(폰 화면)에서는 이 로직이 아무 영향 없음(항상 폭이 430px보다 좁으므로)
- [x] `[검증됨]` `npx tsc --noEmit` 실행 — 에러 0건
- [x] `[검증됨]` `backend/venv/Scripts/python -m pytest`(백엔드 유닛테스트) 실행 — 15개 전부 통과
- [x] `[검증됨]` 실제 백엔드 기동 → `/health` 응답 확인 → 위 1번 항목대로 화면 순회까지 완료 (항목 1과 중복 확인)
- [ ] 발표에 쓸 실기기(또는 노트북 웹)에서 EAS 빌드나 Expo Go로 직접 한 번 더 순회 (이번 검증은 로컬 웹 기준)
- [ ] 발표 직전 앱 완전 종료 후 재시작 → 첫 진입부터 정상 작동하는지 (백그라운드에 오래 떠있던 상태로 시연하다 꼬이는 경우 많음)

## 6. 🟢 발표 보조

- [ ] 만약 라이브 데모가 중간에 실패해도 보여줄 수 있게 화면 녹화본/스크린샷 백업
- [ ] 발표용 기기 화면을 빔프로젝터/화면공유로 미러링하는 케이블·어댑터 미리 확인
- [ ] 백엔드 PC 화면도 필요하면(로그 보여주는 연출 등) 같이 챙기기

---

## 당일 체크 순서 (요약) — 핫스팟 + 노트북 구성

1. 폰 핫스팟 켜고 노트북 연결
2. 백엔드 실행 → 방화벽 팝업 뜨면 "액세스 허용" (안 뜨면 위 2번 항목의 수동 등록 절차대로)
3. 노트북 `ipconfig`로 핫스팟 상의 새 IP 확인 → 다른 기기로 `http://<그 IP>:8000/health` 열려서 확인
4. ESP32 각 `config.h`의 `API_BASE_URL`을 그 IP로 갱신 + 재업로드, 전원 인가 → 서버에 값 올라오는지 확인
5. `seed_demo.py`로 폴백용 더미 데이터 미리 채워두기 (혹시 몰라서)
6. 노트북에서 앱(웹) 열기 — `.env`는 `localhost:8000` 그대로 두면 됨, 자동으로 폰 비율로 보임
7. 시연 시나리오 한 번 리허설 (처음 화면부터 끝까지)
8. 발표 5분 전 앱 재시작 + 핫스팟 재확인, 폰 배터리 확인
